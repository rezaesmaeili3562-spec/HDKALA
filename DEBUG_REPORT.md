# گزارش دیباگ و اصلاح باگ‌ها — HDKALA 🐛✅

تاریخ: ۱۴۰۵/۰۵/۲۵ (2026-08-16)

این سند خلاصه‌ای از باگ‌هایی است که در پروژه HDKALA پیدا و اصلاح شدند. هر باگ با روش بازتولید، ریشه‌ی مشکل و فیکس اعمال‌شده توضیح داده شده است.

---

## 🔴 باگ‌های بحرانی

### ۱. شکست کامل سایت با دستور اجرای README (`npm run server`)
**علائم:** اجرای `npm run server` و باز کردن `http://localhost:3000` → همه فایل‌های CSS و JS با خطای 404 لود می‌شوند و سایت بدون استایل و بدون منطق رندر می‌شود.

**ریشه:** اسکریپت `server` پوشه‌ی `store/templates` را سرو می‌کرد، در حالی که قالب‌ها با مسیر نسبی `../css/...` و `../scripts/...` به پوشه‌های بالاتر ارجاع می‌دهند.

**فیکس:**
- `package.json`: `"server": "npx serve store -l 3000"`
- فایل جدید `store/index.html` که به `templates/home.html` ریدایرکت می‌کند.
- به‌روزرسانی دستورالعمل اجرا در `README.md` و اصلاح ساختار پروژه در آن.

---

### ۲. دکمه «پرداخت و ثبت سفارش» مرده در صفحه تسویه حساب
**علائم:** در صفحه `checkout.html` کلیک روی دکمه پرداخت هیچ کاری انجام نمی‌دهد؛ ثبت سفارش از این صفحه عملاً غیرممکن بود.

**ریشه:** `checkout.html` فقط `checkout.js` را لود می‌کرد، در حالی که نسخه‌ی کامل صفحه تسویه (با دکمه `#finalCheckoutBtn` و منطق ثبت سفارش) در `cart.js` قرار دارد و فقط روی `cart.html` لود می‌شد. نسخه‌ای که روی checkout رندر می‌شد (`renderCheckoutPage` در `ui.js`) دکمه‌ای بدون `id` و بدون هیچ event listener داشت.

**فیکس:**
- افزودن `<script src="../scripts/scripts-pages/cart.js" defer>` به `checkout.html` تا نسخه پیشرفته تسویه جایگزین نسخه ساده شود.
- گارد برای کاربر مهمان (اگر وارد نشده باشد → هدایت به صفحه ورود) و انتخاب نکردن روش پرداخت در `cart.js`.

---

### ۳. کرش پنل ادمین داخل برنامه (`#admin`) — خطای `setupBlogManagement is not defined`
**علائم:** باز کردن صفحه ادمین داخل SPA با خطای `ReferenceError: setupBlogManagement is not defined` نیمه‌کاره رندر می‌شد. (توابع `formatAdminDate`، `getOrderStatusLabel`، `getOrderStatusBadgeClass` و ... هم تعریف نشده بودند.)

**ریشه:** همه‌ی این توابع در `store/scripts/scripts-pages/admin.js` تعریف شده‌اند، اما **هیچ قالبی این فایل را لود نمی‌کرد**! لودر فقط ماژول‌های `scripts-shared` را بارگذاری می‌کرد.

**فیکس:** افزودن `admin.js` به انتهای لیست ماژول‌های لودر (`loader.js`). حالا ورود ادمین (مودال → OTP → session) و گیت `ensureAdminAccess` هم درست کار می‌کنند.

---

### ۴. شکسته شدن لینک‌های مستقیم (Deep Link) به دلیل بازنویسی hash
**علائم:** باز کردن `home.html#product:p1` به جای صفحه محصول، صفحه خانه را نشان می‌داد؛ `product.html#product:p2` پیام «محصول مورد نظر یافت نشد» می‌داد.

**ریشه:** اسکریپت هر صفحه (مثلاً `home.js`) هنگام لود، hash را به زور به هدف خودش (`#home`) تغییر می‌داد، حتی وقتی کاربر با یک hash معنی‌دار (مثل `#product:p1`) وارد شده بود. علاوه بر آن روتر برای رندر اولیه فقط به رویداد `load` متکی بود.

**فیکس:**
- اصلاح هر ۱۶ اسکریپت صفحه: hash فقط وقتی مقداردهی می‌شود که خالی باشد؛ در غیر این صورت `renderPage()` صدا زده می‌شود.
- `router.js`: ناوبری اولیه از طریق صف init (`onDomReady`) اجرا می‌شود و تا لود کامل ماژول‌ها رویدادهای `hashchange` نادیده گرفته می‌شوند (جلوگیری از خطاهای `renderCheckoutPage is not defined` و مشابه).

---

### ۵. اجرا نشدن لیسنرهای `DOMContentLoaded` ماژول‌های داینامیک
**علائم:** بج سبد خرید، لیست علاقه‌مندی و مقایسه بعد از رفرش خالی می‌ماندند؛ لیسنرهای دکمه‌ها (باز شدن سایدبار سبد، فیلترها و...) متصل نمی‌شدند.

**ریشه:** لودر ماژول‌ها را با `defer` به صورت داینامیک به `head` اضافه می‌کند. اسکریپت‌های داینامیک رویداد `DOMContentLoaded` را بلاک نمی‌کنند؛ در نتیجه رویداد DCL **قبل از اجرای ماژول‌ها** شلیک می‌شد و همه‌ی `addEventListener('DOMContentLoaded', ...)` ها (در ۹ فایل) عملاً کد مرده بودند.

**فیکس:**
- تعریف helper سراسری `onDomReady` و صف `__domReadyQueue` در `dom.js`.
- لودر بعد از اجرای آخرین ماژول، صف را flush می‌کند.
- تبدیل همه‌ی لیسنرهای DCL ماژول‌ها به `onDomReady(...)`.
- در `storage.js`: مقداردهی اولیه `updateCartBadge()`, `updateWishlistBadge()`, `updateCompareBadge()` از localStorage.

---

## 🟠 باگ‌های مهم

### ۶. آدرس اول هرگز پیش‌فرض نمی‌شد → بلاک شدن ثبت سفارش
**علائم:** کاربری که چک‌باکس «آدرس پیش‌فرض» را نمی‌زد، موقع ثبت سفارش با پیام «لطفا یک آدرس برای ارسال انتخاب کنید» بلاک می‌شد.

**ریشه:** در `saveAddress`، برای آدرس جدید `addressId` برابر `null` بود و حلقه‌ی «حذف پیش‌فرض از بقیه» با شرط `addr.id !== addressId` خود آدرس جدید را هم غیرپیش‌فرض می‌کرد! (چون id واقعی آدرس هیچ‌وقت با null برابر نیست.)

**فیکس:**
- اگر کاربر هیچ آدرس پیش‌فرضی نداشته باشد، اولین آدرس به‌طور خودکار پیش‌فرض می‌شود.
- اصلاح شرط حلقه با استفاده از شناسه‌ی واقعی آدرس جدید.

---

### ۷. دکمه «ذخیره تغییرات» پروفایل بدون هندلر
**علائم:** کلیک روی دکمه، فرم را به صورت native سابمیت می‌کرد (رفرش صفحه) و تغییرات ذخیره نمی‌شد؛ ایمیل هم پیش‌فرض پر نمی‌شد.

**فیکس:** فرم `id="profileForm"` گرفت؛ هندلر submit نام و ایمیل را اعتبارسنجی و در `HDK_user` ذخیره می‌کند و `updateUserLabel()` را صدا می‌زند. ایمیل از مقدار ذخیره‌شده پیش‌پر می‌شود.

---

### ۸. فرم «تماس با ما» بدون هندلر
**علائم:** سابمیت فرم باعث رفرش صفحه (GET native) می‌شد و هیچ بازخوردی نمایش نمی‌داد.

**فیکس:** `id="contactForm"` + هندلر submit با `preventDefault`، اعتبارسنجی ایمیل و نمایش نوتیفیکیشن موفقیت.

---

### ۹. باز نشدن سایدبار سبد خرید هنگام افزودن محصول
**علائم:** بعد از کلیک «افزودن به سبد» فقط toast نمایش داده می‌شد و کاربر بازخورد بصری سبد (کشوی سبد) را نمی‌دید.

**فیکس:** در `addToCart` بعد از به‌روزرسانی نمایش، سایدبار سبد (`#cartSidebar`) باز می‌شود.

---

## 🟡 باگ‌های جزئی

### ۱۰. `src=""` خالی در تصاویر قالب‌ها
`<img src="">` باعث می‌شود مرورگر آدرس خود صفحه را به عنوان تصویر درخواست کند (درخواست اضافه + خطای کنسول). در هر ۱۶ قالب، `src=""` از تصاویر `product-image` و `blog-image` حذف شد (JS خودش src را مقداردهی می‌کند).

### ۱۱. README قدیمی
ساختار `dist/` در README با ساختار واقعی `store/` هم‌خوان نبود؛ اصلاح شد.

---

## 🧪 تست‌های انجام‌شده (Playwright + Chromium)

| تست | نتیجه |
|---|---|
| بارگذاری ۱۷ صفحه فروشگاه و ادمین (بدون خطای کنسول/404) | ✅ |
| Deep-link: `home.html#product:p1`، `product.html#product:p2`، `#cart` | ✅ |
| افزودن به سبد → بج، سایدبار، افزایش تعداد، ماندگاری بعد از رفرش | ✅ |
| مقایسه دو محصول → مودال | ✅ |
| علاقه‌مندی‌ها → بج و صفحه | ✅ |
| جستجو و فیلتر محصولات | ✅ |
| ورود OTP → تکمیل ثبت‌نام → ذخیره کاربر | ✅ |
| ثبت آدرس → پیش‌فرض خودکار | ✅ |
| تسویه حساب → ثبت سفارش → نمایش در سفارش‌ها | ✅ |
| ذخیره پروفایل، فرم تماس، دارک‌مود، منوی موبایل | ✅ |
| ورود ادمین (مودال → OTP → session → پنل) | ✅ |
| پنل ادمین: ساب‌منوها، ۱۳ صفحه، افزودن محصول | ✅ |
| پنل ادمین SPA (گیت دسترسی) | ✅ |

## 📁 فایل‌های تغییر یافته
- `store/scripts/loader/loader.js` — لود `admin.js` + فلاش صف init
- `store/scripts/scripts-shared/dom.js` — helper `onDomReady` + صف init
- `store/scripts/scripts-shared/router.js` — ناوبری اولیه امن + گیت `routerReady`
- `store/scripts/scripts-shared/storage.js` — init بج‌ها از localStorage + باز شدن سایدبار سبد
- `store/scripts/scripts-shared/pages.js` — فیکس آدرس پیش‌فرض
- `store/scripts/scripts-shared/ui.js` — هندلر فرم پروفایل و تماس
- `store/scripts/scripts-pages/cart.js` — گاردهای ثبت سفارش + صف init زودهنگام
- `store/scripts/scripts-pages/*.js` (۱۶ فایل) — حفظ hash لینک‌های مستقیم
- `store/templates/checkout.html` — لود `cart.js`
- `store/templates/*.html` — حذف `src=""`
- `store/index.html` (جدید) — ریدایرکت به فروشگاه
- `package.json` و `README.md` — اصلاح اسکریپت سرور و مستندات
