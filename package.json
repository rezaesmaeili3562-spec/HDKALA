{
  "name": "hdkala-shop",
  "version": "1.0.0",
  "scripts": {
    "copy:static": "node scripts/sync-static.js",
    "css:dev": "node ./node_modules/tailwindcss/lib/cli.js -i ./src/static/css/input.css -o ./src/static/css/output.css --watch",
    "css:build": "node ./node_modules/tailwindcss/lib/cli.js -i ./src/static/css/input.css -o ./src/static/css/output.css --minify",
    "dev": "npm run copy:static && npm run css:dev",
    "build": "npm run css:build && npm run copy:static",
    "generate:pages": "npm run css:build && npm run copy:static && node scripts/generate-pages.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
